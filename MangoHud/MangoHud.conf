# Конфиг только с одной видеокартой
legacy_layout=0
horizontal
hud_compact

# Время
time
time_no_label
time_format=%H:%M

# CPU
cpu_stats
cpu_temp
cpu_text=CPU

# GPU - только одна видеокарта (обычно дискретная)
gpu_stats
gpu_temp
gpu_text=GPU
# Оставляем только GPU0 (интегрированную) или GPU1 (дискретную)
gpu_list=1

# FPS
fps
# Отключаем frametime (значение в ms)
frametime=0

# Внешний вид
background_alpha=0
text_outline
font_size=20
position=top-left

# Горячая клавиша для включения/выключения оверлея
toggle_hud=F1
